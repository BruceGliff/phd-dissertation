\definecolor{MossGreen}{HTML}{7C9A5B}
\definecolor{MossGreenPale}{HTML}{96ae7c}
\definecolor{Olive}{HTML}{B3A24C}
\definecolor{OlivePale}{HTML}{c2b570}

\begin{tikzpicture}[
    node distance=0.5cm and 0.5cm,
    box/.style={
      rectangle,
      draw,
      very thick,
      inner sep=3pt,
      font=\small,
      align=center
    },
    edge/.style={->, very thick, >={Latex[length=2.5mm]}}
  ]

  \node[box, path picture={{\shade[left color=MossGreenPale, right
        color=OlivePale] ($(path picture bounding box.north
        west)!0.0000!(path picture bounding box.north east)$) rectangle
        ($(path picture bounding box.south west)!1.0000!(path picture
  bounding box.south east)$);}}]  (Node52)
  {$R22_{3}^{1}$};

  \node[box, below=of Node52, fill={MossGreenPale}]  (Node53)
  {$R02_{2}^{2}$};

  \node[box, right=of Node53, fill={OlivePale}]  (Node55)
  {$R10_{3}^{1}$};

  \node[box, below=of Node53, fill={MossGreenPale}]  (Node54)
  {$R10_{2}^{1}$};

  \node[box, below=of Node55, fill={OlivePale}]  (Node56)
  {$R02_{2}^{3}$};

  \draw[edge] (Node53) -- (Node52);
  \draw[edge] (Node55) -- (Node52);
  \draw[edge] (Node54) -- (Node53);
  \draw[edge] (Node56) -- (Node55);

\end{tikzpicture}
