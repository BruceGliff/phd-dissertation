\begin{tikzpicture}[
    node distance=1.5cm and 2cm,
    every node/.style={circle, draw, minimum size=9mm, inner sep=0pt},
    >=stealth
]

% Top layer (outputs) — удалены 4 и 13
\node (O1) {5};
\node (O2) [right=of O1] {6};
\node (O3) [right=of O2] {7};
\node (O4) [right=of O3] {8};

% Middle-top layer — удалены 9 и 13
\node (F) [below=of O2] {10};
\node (G) [below=of O3, xshift=-1cm] {11};
\node (H) [below=of O3, xshift=1cm] {12};

% Middle-bottom layer
\node (C) [below=of F] {3};
\node (D) [below=of H] {2};

% Bottom layer
\node (S) [below=of C, xshift=1.5cm] {1};

% Edges — направления развёрнуты (сверху вниз)
\draw[->] (C) -- (S);
\draw[->] (D) -- (S);

\draw[->] (F) -- (C);
\draw[->] (G) -- (C);

\draw[->] (F) -- (D);
\draw[->] (H) -- (D);

\draw[->] (O1) -- (F);
\draw[->] (O2) -- (F);
\draw[->] (O3) -- (F);

\draw[->] (O2) -- (G);
\draw[->] (O4) -- (G);

\draw[->] (O3) -- (H);
\draw[->] (O4) -- (H);

\end{tikzpicture}
