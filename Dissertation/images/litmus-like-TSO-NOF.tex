\begin{tikzpicture}[
    node distance=1.5cm and 1.5cm,
    >=Stealth,
    every node/.style={on grid, rectangle, draw, minimum size=8mm}
]

% Define nodes
\node (Wx) {$\text{W}_x$};
\node[below=of Wx] (Ry) {$\text{R}_y$};

\node[right=of Wx] (Wy) {$\text{W}_y$};
\node[below right=of Ry] (Rx) {$\text{R}_x$};

% Black dashed arrows - on the left side
\draw[black, dashed, ->] (Wx) -- (Ry);
\draw[black, dashed, ->] (Wy) to[bend right] (Rx);

% Red arrows (solid) - on the right side
\draw[red, thick, ->] (Wy) -- (Rx);


\end{tikzpicture}
